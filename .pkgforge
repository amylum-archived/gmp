name 'gmp'
org 'amylum'

licenses 'COPYING'

configure_flags(
  prefix: '/usr',
  'enable-maintainer-mode': nil,
  'disable-padlock-support': nil
)

harden

build do
  configure
  make
  install
end

test do
  # TODO: add tests
end
